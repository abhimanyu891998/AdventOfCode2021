#ifndef ADVENTOFCODE_UTILS
#define ADVENTOFCODE_UTILS

#include <fstream>
#include <vector>
#include<string>

namespace util
{
        std::vector<std::string> split(const std::string &s, char delim);
}

#endif